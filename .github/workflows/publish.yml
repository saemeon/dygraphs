name: Publish

on:
  release:
    types: [published]

permissions:
  id-token: write

jobs:
  deploy:
    name: Publish to PyPI
    runs-on: ubuntu-latest
    permissions:
      id-token: write      # trusted publishing
      contents: read
    environment:
      name: pypi
      url: https://pypi.org/p/dygraphs
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up uv
        uses: astral-sh/setup-uv@v7
        with:
          python-version: "3.13"

      - name: Build
        run: uv build

      - name: Publish
        uses: pypa/gh-action-pypi-publish@release/v1
